image:
  file: .gitpod.Dockerfile
tasks:
  - command: pip install numpy
  - command: pip install qmsolve[with_mayavi]
  - command: echo "Hello world!"
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    pullRequestsFromForks: true
    addCheck: true
    addComment: false
